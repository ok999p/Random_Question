// const questions = [
//   "ความมั่นใจ (Passion/Confidence) ",
//   "จิตอาสา (Volunteering) ",
//   "ชีวิตนักศึกษา (Student Life) ",
//   "กิจกรรมนักศึกษา (Student Activities) ",
//   "ค่ายอาสา (Volunteer Camp) ",
//   "การศึกษาออนไลน์ (Online Learning) ",
//   "โอกาสทางการศึกษา (Educational Opportunities) ",
//   "WU FRESHY AWARDS ",
//   "ทัศนคติ (Attitude) ",
//   "การเรียนรู้ในยุคดิจิทัล (Learning in the Digital Era) ",
//   "ภาวะผู้นำ (Leadership) ",
//   "มหาวิทยาลัยสีเขียว (Green University) ",
//   "สังคมในมหาวิทยาลัย (University Community) ",
//   "ความเหลื่อมล้ำ (Inequality) ",
//   "สมรสเท่าเทียม (Marriage Equality) ",
//   "ความล้มเหลว (Failure) ",
//   "อนาคต (Future) ",
//   "ความสัมพันธ์ที่เป็นพิษ (Toxic Relationship) ",
//   "ความเป็นมืออาชีพ (Professionalism) ",
//   "ความเท่าเทียม (Equality) ",
//   "ความมั่นใจในตนเองสูง (High self-Confidence) ",
//   "ทรัพย์สินทางปัญญา (Intellectual Property) ",
//   "ใบโพลาร์ (Bipolar) ",
//   "สิทธิขั้นพื้นฐาน (Fundamental Rights) ",
//   "Soft Power ",
//   "Influencers ",
//   "อำนาจเงิน (Financial Power) ",
//   "ความสำเร็จ (Success) ",
//   "Safe Zone ",
//   "New Gen ",
//   "การรับฟัง (Listening) ",
//   "แรงบันดาลใจ (Inspiration) ",
//   "คุณค่าแห่งตน (Self-Worth) ",
//   "การคุกคามทางเพศ (Sexual Harassment) ",
//   "ความรัก (Love) ",
//   "เสรีภาพ (Freedom) ",
//   "การให้เกียรติ (Respectfulness) ",
//   "ความเป็นเลิศสู่สากล (Excellence to Global Standards) ",
//   "การเหยียด (Bully/Discrimination) ",
//   "ความกล้าหาญ (Courage) ",
//   "ความเคารพ (Respect)",
//   "ศรัทธา (Faith) ",
//   "วุฒิภาวะ (Maturity) ",
//   "บ้าน (Home) ",
//   "การยอมรับความจริง (Acceptance of Reality) ",
//   "ความเป็นตัวเอง (Being True to Oneself) ",
//   "โรคซึมเศร้า (Depression) ",
//   "โรคติดต่อทางเพศสัมพันธ์ (Sexually Transmitted Diseases) ",
//   "กาลเทศะ (Appropriateness) ",
//   "โลกสวย (Optimism) "
// ];
const questions = [
  "คุณมีความคิดเห็นอย่างไรเกี่ยวกับกฎหมายสมรสเท่าเทียม?",
  "คุณคิดว่ามหาวิทยาลัยวลัยลักษณ์มีจุดเด่นหรือเอกลักษณ์อย่างไร เพราะเหตุใด?",
  "หากคุณได้รับตำแหน่ง WU Freshy Awards 2024 คุณคิดว่าคุณสามารถสร้างแรงบันดาลใจให้ผู้อื่นได้อย่างไร?",
  "ความเป็นผู้นำในมุมมองของคุณมีลักษณะอย่างไร? และคุณจะเป็นผู้นำที่ดีได้อย่างไร?",
  "หากคุณต้องทำงานกลุ่มกับเพื่อนในห้องเรียน และพบว่าเพื่อนในกลุ่มของคุณไม่สามารถทำงานร่วมกันได้อย่างราบรื่น คุณจะทำอย่างไร?",
  "ถ้าคุณเป็นส่วนหนึ่งของต้นไม้ใหญ่ คุณจะเลือกเป็นส่วนใด เพราะเหตุใด?",
  "ถ้าคุณต้องเลือกระหว่างสิ่งที่ต้องทำ สิ่งที่ควรทำ และสิ่งที่อยากทำ คุณจะทำสิ่งไหนก่อน เพราะเหตุใด?",
  "ถ้าคุณได้รับตำแหน่ง WU Freshy Awards 2024 แล้วมีการวิพากษ์วิจารณ์ในทางลบ คุณจะมีวิธีการจัดการอย่างไร?",
  "หากคุณได้รับตำแหน่ง WU Freshy Awards 2024 คุณจะทำอย่างไรให้ทุกคนรู้จักและเห็นความสำคัญของตำแหน่งที่คุณได้รับจนกว่าจะยกตำแหน่งให้รุ่นต่อไป?",
  "ในยุคที่โซเชียลมีเดียมีอิทธิพลต่อสังคมอย่างมาก คุณคิดว่าคุณจะมีบทบาทอย่างไรในการสร้างแรงบันดาลใจและส่งเสริมพฤติกรรมที่ดีผ่านช่องทางออนไลน์?",
  "หากคุณได้รับตำแหน่ง WU Freshy Awards 2024 คุณจะมีวิธีการประชาสัมพันธ์มหาวิทยาลัยให้คนภายนอกรู้จักอย่างไร?",
  "หากคุณไม่ได้รับตำแหน่ง WU Freshy Awards 2024 คุณจะมีวิธีจัดการกับความรู้สึกตนเองอย่างไร?",
  "ค่านิยม 5 ประการ ได้แก่ รักชาติ กตัญญู รู้วินัย ใจอาสา พัฒนาภาวะผู้นำ คุณคิดว่าคุณสมบัติข้อใดสำคัญที่สุด เพราะเหตุใด?",
  "อะไรคือความกลัวที่ยิ่งใหญ่ที่สุดของคุณ และคุณจะเอาชนะมันอย่างไร?",
  "มหาวิทยาลัยวลัยลักษณ์ได้รับการจัดอันดับ 1 มหาวิทยาลัยสีเขียวของภาคใต้ 4 ปีซ้อน ในฐานะที่คุณเป็นนักศึกษาของมหาวิทยาลัยวลัยลักษณ์ คุณจะส่งเสริม Green University ได้อย่างไร?",
  "คุณเห็นด้วยหรือไม่ หากนักศึกษาสามารถเข้าเรียนได้โดยไม่จำเป็นที่จะต้องใส่ชุดนักศึกษา?",
  "คุณคิดว่าความงามที่แท้จริงคืออะไร?",
  "อะไรที่ทำให้คุณแตกต่างจากผู้เข้าประกวดคนอื่นๆ บนเวทีนี้?",
  "คุณให้ความหมายกับคำว่า 'ความสำเร็จ' อย่างไร?",
  "ในระหว่างที่เรียนอยู่ที่มหาวิทยาลัยวลัยลักษณ์ คุณได้เรียนรู้อะไรที่มีคุณค่าต่อการพัฒนาตนเองบ้าง?",
  "คุณมีวิธีจัดการกับความกดดันในการเป็นตัวแทนของมหาวิทยาลัยอย่างไรบ้าง?",
  "คุณอยากให้เพื่อนนักศึกษาจดจำคุณในฐานะดาวเดือนมหาวิทยาลัยวลัยลักษณ์ว่าเป็นคนแบบใด?",
  "คุณคิดว่าอะไรคือแรงผลักดันภายในตัวคุณที่ทำให้คุณพยายามและมุ่งมั่นที่จะเป็นดาวเดือนมหาวิทยาลัยวลัยลักษณ์?",
  "ถ้าคุณต้องเลือกระหว่างการเป็นตัวเองและการทำให้ผู้อื่นพอใจ คุณจะเลือกทางใดและเพราะอะไร?",
  "คุณคิดว่าอะไรเป็นปัจจัยที่ทำให้คนเรามีความสุขในชีวิตจริง?",
  "คุณเคยมีประสบการณ์ที่ต้องก้าวออกจากพื้นที่ปลอดภัยของตัวเองหรือไม่? ถ้ามี คุณได้เรียนรู้อะไรจากประสบการณ์นั้น?",
  "คุณมีวิธีจัดการกับการเปรียบเทียบตัวเองกับคนอื่นๆ อย่างไร และทำอย่างไรถึงจะรักษาความมั่นใจในตนเองได้?",
  "คุณให้ความสำคัญกับการรักษาสุขภาพจิตใจอย่างไรเมื่ออยู่ในสภาพแวดล้อมที่มีการแข่งขันสูง?",
  "คุณคิดว่าคุณสมบัติใดที่สำคัญที่สุดในการที่จะได้รับตำแหน่ง WU Freshy Awards 2024 และเพราะอะไรคุณถึงคิดเช่นนั้น?",
  "หากมีนักเรียนมัธยมศึกษาตอนปลายสอบถามคุณเกี่ยวกับหลักสูตรที่น่าสนใจของมหาวิทยาลัย คุณจะแนะนำหลักสูตรใดเป็นอันดับแรก และเหตุผลที่คุณเลือกหลักสูตรนั้นคืออะไร?"
];






let isSpinning = false;
let animationFrame;
let selectedQuestion = "";
let usedQuestions = new Set();
let lightSpeed = 4; // ความเร็วเริ่มต้นของแสง (หน่วยเป็นวินาที)
const MAX_LENGTH = 25; // กำหนดความยาวสูงสุดเป็น 25

function getRandomQuestion() {
    const availableQuestions = questions.filter(q => !usedQuestions.has(q));
    if (availableQuestions.length === 0) {
        return "หมดคำถาม";
    }
    const question = availableQuestions[Math.floor(Math.random() * availableQuestions.length)];
    usedQuestions.add(question);
    return question;
}

function animateSpinning() {
  const element = document.getElementById('questionDisplay');
  const thaiChars = "@#$^%*^(&)*_(_++))^&^%#$@@#!@!##$%^^*&@#$%^&*()_+_)(*&^%$#@#$%^&*()_+_)(*&^%$#";
  const animationLength = Math.min(selectedQuestion.length, MAX_LENGTH);

  function spin() {
      if (!isSpinning) return;
      element.innerText = Array(animationLength).fill()
          .map(() => thaiChars[Math.floor(Math.random() * thaiChars.length)])
          .join("");
      animationFrame = requestAnimationFrame(spin);
  }
  spin();
}

function stopSpinning() {
    isSpinning = false;
    cancelAnimationFrame(animationFrame);
    animateFinalQuestion(selectedQuestion);
}

function animateFinalQuestion(question) {
  const element = document.getElementById('questionDisplay');
  const startTime = Date.now();
  const duration = 2000;
  const letters = question.split("");
  const steps = letters.length;
  const map = (n, x1, y1, x2, y2) => Math.min(Math.max(((n - x1) * (y2 - x2)) / (y1 - x1) + x2, x2), y2);
  const thaiChars = "@#$^%*^(&)*_(_++))^&^%#$@@#!@!##$%^^*&@#$%^&*()_+_)(*&^%$#@#$%^&*()_+_)(*&^%$#";
  const random = () => thaiChars[Math.floor(Math.random() * thaiChars.length)];

  function animate() {
      const progress = (Date.now() - startTime) / duration;
      const step = Math.round(progress * steps);
      const currentLength = Math.min(MAX_LENGTH + Math.floor(progress * (steps - MAX_LENGTH)), steps);

      element.innerText = letters
          .slice(0, currentLength)
          .map((s, i) => (i < step ? s : random()))
          .join("");

      if (progress >= 1) {
          cancelAnimationFrame(animationFrame);
      } else {
          animationFrame = requestAnimationFrame(animate);
      }
  }
  animate();
}

function toggleSpinning() {
    const button = document.getElementById('generateButton');
    const goldenLight = document.querySelector('.golden-light');

    if (!isSpinning) {
        selectedQuestion = getRandomQuestion();
        if (selectedQuestion === "หมดคำถาม") {
            document.getElementById('questionDisplay').innerText = selectedQuestion;
            button.disabled = true;
            return;
        }
        isSpinning = true;
        button.textContent = 'Stop';
        animateSpinning();
        
        // เพิ่มความเร็วของแสง
        lightSpeed = 0.5; // ความเร็วสูงสุดของแสง
        goldenLight.style.setProperty('--light-speed', `${lightSpeed}s`);
        goldenLight.classList.add('spinning');
    } else {
        button.textContent = 'Generate Question';
        stopSpinning();
        
        // ค่อยๆ ลดความเร็วของแสง
        slowDownLight();
    }
}

function slowDownLight() {
    const goldenLight = document.querySelector('.golden-light');
    
    if (lightSpeed < 4) {
        lightSpeed += 0.2;
        goldenLight.style.setProperty('--light-speed', `${lightSpeed}s`);
        requestAnimationFrame(slowDownLight);
    } else {
        goldenLight.classList.remove('spinning');
    }
}

document.addEventListener('DOMContentLoaded', () => {
    const button = document.getElementById('generateButton');
    button.addEventListener('click', toggleSpinning);
    document.addEventListener('keydown', (event) => {
        if (event.code === 'Space') {
            event.preventDefault();
            toggleSpinning();
        }
    });
});

/* ---- particles.js config ---- */

particlesJS("particles-js", {
    "particles": {
      "number": {
        "value": 100,
        "density": {
          "enable": true,
          "value_area":1000
        }
      },
      "color": {
        "value": ["#aa73ff", "#f8c210", "#83d238", "#33b1f8"]
      },
      
      "shape": {
        "type": "circle",
        "stroke": {
          "width": 0,
          "color": "#fff"
        },
        "polygon": {
          "nb_sides": 5
        },
        "image": {
          "src": "img/github.svg",
          "width": 100,
          "height": 100
        }
      },
      "opacity": {
        "value": 0.6,
        "random": false,
        "anim": {
          "enable": false,
          "speed": 1,
          "opacity_min": 0.1,
          "sync": false
        }
      },
      "size": {
        "value": 2,
        "random": true,
        "anim": {
          "enable": false,
          "speed": 40,
          "size_min": 0.1,
          "sync": false
        }
      },
      "line_linked": {
        "enable": true,
        "distance": 120,
        "color": "#ffffff",
        "opacity": 0.4,
        "width": 1
      },
    },
    "interactivity": {
      "detect_on": "canvas",
      "events": {
        "onhover": {
          "enable": true,
          "mode": "grab"
        },
        "onclick": {
          "enable": false
        },
        "resize": true
      },
      "modes": {
        "grab": {
          "distance": 140,
          "line_linked": {
            "opacity": 1
          }
        },
        "bubble": {
          "distance": 400,
          "size": 40,
          "duration": 2,
          "opacity": 8,
          "speed": 3
        },
        "repulse": {
          "distance": 200,
          "duration": 0.4
        },
        "push": {
          "particles_nb": 4
        },
        "remove": {
          "particles_nb": 2
        }
      }
    },
    "retina_detect": true
  });


  let countdown;
  let timeLeft = 120; // ตั้งค่าเวลาเริ่มต้นที่ 2 นาที (120 วินาที)
  let beepInterval; // กำหนดตัวแปรสำหรับเก็บการวนซ้ำเสียง beep
  
  // ฟังก์ชันอัปเดตการแสดงผลของเวลา
  function updateTimerDisplay() {
      const minutes = String(Math.floor(timeLeft / 60)).padStart(2, '0');
      const seconds = String(timeLeft % 60).padStart(2, '0');
      document.getElementById('timerDisplay').innerText = `${minutes}:${seconds}`;
  }
  
  // ฟังก์ชันเริ่มจับเวลา
  function startTimer() {
      if (countdown) clearInterval(countdown);
      countdown = setInterval(() => {
          if (timeLeft > 0) {
              timeLeft--;
              updateTimerDisplay();
          } else {
              clearInterval(countdown);
              playBeepSound(); // เล่นเสียงเมื่อหมดเวลา
          }
      }, 1000);
  }
  
  // ฟังก์ชันเล่นเสียงเมื่อหมดเวลา
  function playBeepSound() {
    const beepSound = new Audio('./Beep2.mp3');
    beepSound.play();
    beepInterval = setInterval(() => {
        beepSound.play();
    }, 3000);
}
  
  // ฟังก์ชันหยุดจับเวลา
  function stopTimer() {
    clearInterval(countdown);
    clearInterval(beepInterval);
}
  
  
  function resetTimer() {
      timeLeft = 120; 
      updateTimerDisplay();
      clearInterval(beepInterval); 
  }
  
  // กำหนดการทำงานของปุ่ม
  document.getElementById('startTimerButton').addEventListener('click', startTimer);
  document.getElementById('stopTimerButton').addEventListener('click', stopTimer);
  document.getElementById('resetTimerButton').addEventListener('click', resetTimer);
  
  // เรียกฟังก์ชันแสดงเวลาเริ่มต้นทันทีเมื่อโหลดหน้าเว็บ
  updateTimerDisplay();
  


function updateTimerDisplay() {
  const minutes = String(Math.floor(timeLeft / 60)).padStart(2, '0');
  const seconds = String(timeLeft % 60).padStart(2, '0');
  const timerDisplay = document.getElementById('timerDisplay');

  // เพิ่มแอนิเมชันทุกครั้งที่เวลาถอยหลัง
  timerDisplay.classList.add('pulse');
  timerDisplay.innerText = `${minutes}:${seconds}`;

  // ลบคลาส 'pulse' หลังจากแอนิเมชันจบ
  setTimeout(() => {
      timerDisplay.classList.remove('pulse');
  }, 500);
}


function resetQuestion() {
  Swal.fire({
      title: 'ต้องการรีเซ็ตคำถามจริงๆ หรือไม่?',
      icon: 'warning',
      showCancelButton: true,
      confirmButtonText: 'ยืนยัน',
      cancelButtonText: 'ยกเลิก',
      confirmButtonColor: '#d33',
      cancelButtonColor: '#3085d6',
  }).then((result) => {
      if (result.isConfirmed) {
          const questionDisplay = document.getElementById('questionDisplay');
          questionDisplay.innerText = "QUESTION RANDOM"; // ตั้งค่าคำถามเริ่มต้น
          usedQuestions.clear(); // ล้างชุดคำถามที่เคยใช้ไปแล้ว
          document.getElementById('generateButton').disabled = false; // เปิดใช้งานปุ่มสุ่มคำถามอีกครั้ง

          Swal.fire({
              title: 'รีเซ็ตคำถามเรียบร้อยแล้ว!',
              icon: 'success',
              confirmButtonText: 'ตกลง',
              confirmButtonColor: '#3085d6',
          });
      }
  });
}

document.getElementById('resetQuestionButton').addEventListener('click', resetQuestion);
